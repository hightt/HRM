<!DOCTYPE html>

<title>Edit WorkLog</title>

{% block body %}
    <h1>Edit WorkLog</h1>

    {{ include('time_sheet/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_time_sheet_index') }}">back to list</a>

    {{ include('time_sheet/_delete_form.html.twig') }}
{% endblock %}
