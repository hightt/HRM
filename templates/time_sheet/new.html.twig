<!DOCTYPE html>

<title>New WorkLog</title>

{% block body %}
    <h1>Create new WorkLog</h1>

    {{ include('time_sheet/_form.html.twig') }}

    <a href="{{ path('app_time_sheet_index') }}">back to list</a>
{% endblock %}
