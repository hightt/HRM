{% extends 'index.html.twig' %}

{% block title %}
	{{ 'leave_request.views.create.data'|trans }}
{% endblock %}

{% block content %}
	<div class="row">
		<div class="col-12">
			<nav class="navbar navbar-light ">
				<div class="container-fluid">
					<div class="me-auto">
						<span></span>
					</div>
					<span class="navbar-brand mb-0 h1">
						<img src="{{ asset('images/logo2.png')}}" width="175px" height="75px">
					</span>
					<div class="ms-auto"></div>
				</div>
			</nav>
		</div>
	</div>
	{{ form_start(form) }}

	<div class="row">
		<div class="col-12">
			<div class="mb-3 ms-3 me-3">
				<span style="font-size: 23px;">{{ 'leave_request.views.create.data'|trans }}</span>
				<hr class="p-0 mt-1">
			</div>
		</div>
		<div class="col-12">
			<div class="mb-3 ms-3 me-3">
				<label>{{ 'leave_request.views.create.type'|trans }}</label>
				{{ form_widget(form.status, { 'attr': {'class': 'form-control'} }) }}
				{{ form_errors(form.status) }}
			</div>
			<div class="mb-3 ms-3 me-3">
				<label>
				{{ 'leave_request.views.show.additional_comments'|trans }}</label>
				{{ form_widget(form.managerComment, { 'attr': {'class': 'form-control'} }) }}
				{{ form_errors(form.managerComment) }}
			</div>
		</div>
	</div>

	<div class="text-center">
		<button type="submit" class="btn purple-strong">{{ 'common.actions.save'|trans }}</button>
	</div>
	{{ form_row(form._token) }}
	{{ form_end(form, {'render_rest': false}) }}

{% endblock %}
